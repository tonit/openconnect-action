{
  "name": "openconnect-autoclose",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "package" : "npx ncc build -m index.js && npx ncc build -m post.js --out dist/post",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@vercel/ncc": "^0.38.2"
  },
  "dependencies": {
    "@actions/core": "^1.10.1"
  }
}
